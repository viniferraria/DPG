# CLAUDE.md

Orientation for this repo. Depth lives in the OKF bundle at `okf/docs/` — go there before grepping.

## Working rules

These bias toward caution over speed. On trivial tasks, use judgment.

### Think before coding

- State assumptions explicitly. If uncertain, ask.
- Multiple interpretations exist → present them, don't pick silently.
- A simpler approach exists → say so. Push back when warranted.
- Something is unclear → stop, name what's confusing, ask.

### Simplicity first

Minimum code that solves the problem. Nothing speculative.

- No features beyond what was asked.
- No abstractions for single-use code.
- No flexibility or configurability that wasn't requested.
- No error handling for impossible scenarios.
- 200 lines that could be 50 → rewrite it.

Test: would a senior engineer call this overcomplicated?

### Surgical changes

Touch only what you must. Clean up only your own mess.

- Don't improve adjacent code, comments, or formatting.
- Don't refactor what isn't broken.
- Match existing style, even if you'd do it differently.
- Unrelated dead code → mention it, don't delete it.
- Orphans your change created (unused imports, variables, functions) → remove them.

Test: every changed line traces directly to the request.

### Goal-driven execution

- Turn the task into a verifiable goal: "add validation" → "write tests for invalid inputs, then make
  them pass".
- For multi-step work, state the plan as steps with a verification per step.
- Strong success criteria let you loop independently; "make it work" does not.

### Orchestrator pattern

**Opus 5 plans and drives. Sonnet 5 workers execute in parallel.**

```text
                              ┌─────────────┐
                        ┌────▶│  Worker 1   │↺
   ┌──────────────┐     │     │  Sonnet 5   │
↺  │ Orchestrator │─────┤     └─────────────┘
   │   Opus 5     │  fan │     ┌─────────────┐
   │    (plan)    │  out ├────▶│  Worker 2   │↺
   └──────────────┘     │     │  Sonnet 5   │
     main loop          │     └─────────────┘
                        │     ┌─────────────┐
                        └────▶│  Worker 3   │↺
                              │  Sonnet 5   │
                              └─────────────┘
                                 worker loop
```

- **Orchestrator (Opus 5)** owns the plan, the decomposition, and the judgment calls. It holds context,
  verifies what comes back, and is the only party that talks to the user. It doesn't do the bulk reading.
- **Workers (Sonnet 5)** each loop over one scoped task with an explicit brief, the files they own, and
  the expected output shape.
- Partition work by file or directory — two workers writing one file is a self-inflicted conflict.
- Brief workers with constraints, not just goals. They start cold, with no conversation context.
- Require claims to come from files actually read; uncertain items get omitted, not guessed.
- Worker output is a claim, not a result. Spot-check strong assertions before passing them on.
- Don't fan out for trivial work — a single-file edit is cheaper done directly.

Reusable worker definitions live in `.claude/agents/`.

**These rules are working if:** fewer unnecessary changes in diffs, fewer rewrites from overcomplication,
and clarifying questions arrive before implementation rather than after mistakes.

## Project knowledge: the OKF bundle

`okf/docs/` is an [Open Knowledge Format](https://github.com/GoogleCloudPlatform/knowledge-catalog) v0.2
bundle — markdown files with YAML frontmatter, readable by any agent or human, no tooling required. It is
the deep reference; this file is the quick orientation. Start at `okf/docs/index.md`.

| Question | Concept |
| --- | --- |
| How does the pipeline fit together? | `pipeline.md` |
| What exactly does this module do? | `modules/` — one file per module, with real API tables |
| Why is node identity fragile? | `conventions/label-contract.md` |
| Which config wins, and where do they disagree? | `conventions/config-resolution.md` |
| What do the explanation dataclasses hold? | `concepts/` |
| How do I run, verify, or ship this? | `workflows/` |
| What does an experiment suite do? | `experiments/` |

- The bundle is generated by reading source; every claim traces to a file actually read. Where it and
  this file disagree, trust the bundle and fix the stale claim here.
- Change a module's API, a contract, or a workflow → update the matching concept file in the same change
  and add a dated entry to `okf/docs/log.md`.
- Conformance: every non-reserved `.md` needs parseable frontmatter with a non-empty `type`; `index.md`
  and `log.md` are reserved; links are bundle-relative (`/modules/dpg-core.md`).
- `docs/` is the separate Sphinx + autoapi API reference. The bundle does not replace it.

## Development setup

`uv` manages dependencies (the `[build-system]` is still `poetry-core`). Graph rendering needs the system
Graphviz `dot` binary on `PATH` (`brew install graphviz`).

```bash
uv sync                          # runtime dependencies
uv sync --group dev              # + mypy, ruff, pytest
uv sync --extra docs             # + docs dependencies
```

## Common commands

```bash
# Tests — must run from the repo root (see below)
uv run pytest
uv run pytest tests/test_explainer.py::test_name

# Lint — CI passes an explicit tracked-file list, which keeps ruff out of .venv/ and experiment output
uv run ruff check $(git ls-files '*.py')

# Type checking — only these two packages; disallow_untyped_defs = true
uv run mypy dpg/ metrics/

# Docs
uv run sphinx-build -b html docs/ docs/_build/html

# CLI entrypoint (see "Known breakage" before using run_dpg_custom.py)
uv run python examples/run_dpg_standard.py --ds iris --l 3 --seed 42
```

Run pytest from the repo root: `tests/conftest.py` prepends the repo root to `sys.path` (no install
needed), and `tests/test_integration_dpg.py` resolves `datasets/custom.csv` through `os.getcwd()`. There
is no ruff or pytest configuration anywhere — both run on defaults.

CI (`.github/workflows/`):

- `feature-pr.yaml` — the only workflow that runs. On pushes to `feature/**` or `feature*`, it runs
  `uv sync --locked --dev`, then ruff, mypy, and pytest on Python 3.11, then opens a PR. Nothing runs on
  `main`, on `develop`, or on pull requests.
- `docs.yml` — greps tee'd Sphinx output for `^WARNING:` (excluding the benign `Unknown type: placeholder`)
  and checks `PIPESTATUS[0]`. Effect is warnings-as-errors; the mechanism is not `-W`.
- `ci.yml`, `formater.yaml`, `telegram-on-commit.yml` — commented out line by line, including `name:` and
  `on:`. They never run. `requirements.txt` only matters if `ci.yml` is re-enabled.

## Architecture

Two packages: `dpg/` (core library) and `metrics/` (graph metrics). `metrics/` is imported by `dpg/`, not
the reverse — except a lazy `from dpg.exceptions import DPGMetricError` inside three guards in
`metrics/nodes.py`.

### The pipeline is process-mining shaped

Understand this before touching `core.py`. DPG does not walk trees into a graph directly; it replays
samples through the ensemble to produce an *event log*, then mines a directly-follows graph:

```text
sklearn model + X
  → SklearnEnsembleNormalizer          normalize ensembles to a uniform .estimators_ / .tree_ shape
  → tracing_ensemble(_parallel)        replay each sample down every tree → [case_id, event] pairs
  → DataFrame["case:concept:name", "concept:name"]   the trace log; one case per (sample, tree)
  → filter_log / discover_dfg          count directly-follows pairs → {(src_label, dst_label): frequency}
  → generate_dot                       graphviz.Digraph; node id = str(int(sha1(label), 16))
  → to_networkx                        parses dot.body text back into nx.DiGraph + nodes_list
  → NodeMetrics / EdgeMetrics / GraphMetrics
  → DPGExplanation / DPGLocalExplanation
  → visualizer.py                      render to graphviz / matplotlib
```

Consequences:

- **Node identity is the predicate text.** IDs are `sha1` of the label, so two predicates merge iff their
  labels are byte-identical. `decimal_threshold` therefore controls how much the graph merges.
- **Label formats are a contract** across `core.py`, `metrics/graph.py`, `explainer.py`, and
  `visualizer.py`: `"<feature> <= <threshold>"`, `"<feature> > <threshold>"`, `"Class <name>"`,
  `"Pred <value>"`. Parsing helpers depend on these exact shapes — see
  `okf/docs/conventions/label-contract.md`.
- `to_networkx` round-trips through the DOT *source text*, so changes to label escaping or attribute
  ordering in `generate_dot` can silently break parsing.
- Local explanations use normalized class names (`"0"`) in `class_votes` / `majority_vote`, but keep raw
  labels (`"Class 0"`) in `tree_paths[*].labels`.

### `dpg/`

Per-module API detail lives in `okf/docs/modules/`.

| Module | Owns |
| --- | --- |
| `core.py` | `DecisionPredicateGraph`, `DEFAULT_DPG_CONFIG`. Graph construction and config resolution. |
| `sklearn_normalizer.py` | `SklearnEnsembleNormalizer`. **Only GradientBoosting is normalized** (`GB_MODELS`), flattening the per-class-column layout and handling the binary sign-of-leaf-score case. Every other ensemble is an identity pass-through. |
| `explainer.py` | `DPGExplainer` plus the `DPGExplanation` / `DPGLocalExplanation` / `DPGTreePathExplanation` dataclasses, local tracing, sample confidence, and `evaluate_faithfulness`. |
| `visualizer.py` | Every plot function; `DPGExplainer.plot*` are thin wrappers. Largest module. |
| `sklearn_dpg.py` | Dataset selection + train/evaluate/report glue for the `examples/` CLIs. |
| `themes.py`, `utils.py` | Palette/theme resolution and shared helpers. |

### `metrics/`

`NodeMetrics` and `EdgeMetrics` are stateless staticmethods. `GraphMetrics` defines an `__init__` storing
`target_names`, which no method reads — treat it as stateless too. Signatures differ, so check before
calling:

| Method | Signature |
| --- | --- |
| `NodeMetrics.extract_node_metrics` | `(dpg_model, nodes_list)` |
| `EdgeMetrics.extract_edge_metrics` | `(dpg_model, nodes_list)` |
| `GraphMetrics.extract_graph_metrics` / `_lpa` / `extract_class_boundaries` | `(dpg_model, nodes_list, target_names)` |
| `GraphMetrics.extract_communities` | `(dpg_model, df_node_metrics, nodes_list, threshold_clusters=0.2)` |

`nodes_list` is the `[node_id, label]` list from `to_networkx`. `metrics/nodes.py` converts to `igraph`
(`_nx_to_igraph`) for betweenness and reaching centrality — that conversion is the hot path.

## Config

Resolution order in `DecisionPredicateGraph.__init__`: explicit `dpg_config` (dict or OmegaConf
`DictConfig`) → `config.yaml` resolved **relative to the current working directory** →
`DEFAULT_DPG_CONFIG`. Fallback is per key, not whole-object.

| | `config.yaml` | `DEFAULT_DPG_CONFIG` |
| --- | --- | --- |
| `perc_var` | 0.0001 | 1e-9 |
| `decimal_threshold` | 3 | 6 |

Both set `n_jobs: -1`. `config.yaml` has no `graph_construction` section, so the mode always falls back to
the default. Because `decimal_threshold` controls label rounding and node identity, running from a
different directory changes node counts, not just filtering — pass `dpg_config` explicitly in tests and
experiments.

### Graph construction modes

`dpg_config["dpg"]["graph_construction"]["mode"]`:

- `"aggregated_transitions"` (default) — `filter_log` drops whole path *variants* below
  `n_cases * perc_var`, then builds the DFG.
- `"execution_trace"` — builds the DFG from the raw log, then drops individual *edges* below the same
  threshold.

Unsupported mode strings raise `DPGError` at construction time.

`explain_local` always re-traces the sample through the raw trees (reported as
`path_mode="execution_trace"`), then checks each traced node/edge against the built graph. An aggressive
`perc_var` therefore makes `graph_path_valid` / `all_trees_valid` go False — that is filtering working as
intended, not a tracing bug.

## Other directories

- `examples/` — runnable scripts and the two CLIs.
- `experiments/` — research runners; linted but not type checked. Three self-contained suites:
  - `causal_synthetic_scenarios/` — `run_dpg_causal_synthetic.py` / `run_dpg_synthetic.py`, `generator/`,
    `datasets/`, `results/`, `states/`. Covered by `tests/test_run_dpg_causal_synthetic.py`.
  - `local_explanation/` — the only suite that is a package (exports `run_local_explanation_experiments`).
    Imported by `tests/test_smoke.py`.
  - `monks/` — `run_monk.py` over the UCI MONK's problems. No test coverage, and **untracked in git**.
  - Runners write timestamped `.log` files and outputs next to themselves. Treat `results/`, `states/`,
    and `*.log` as artifacts, not source.
- `tutorials/` — notebooks, including the `perc_var` / `decimal_threshold` sensitivity benchmark.
- `docs/` — Sphinx + autoapi; see `docs/README.md`.
- `okf/docs/` — the OKF knowledge bundle described above.

## Known breakage

Verified against the source. Mention these rather than tripping over them.

- `examples/run_dpg_custom.py` crashes — it unpacks 2 values from `test_dpg`'s 6-tuple.
- `pyproject.toml` declares the console script as `scripts.run_dpg_standard:main`, but no `scripts/`
  package exists and `examples/run_dpg_standard.py` has no `main()`. The CLI only runs as a script.
- `.readthedocs.yaml` installs `docs/requirements.txt`, which does not exist — RTD builds fail at install.
- `test_datasets/` does not exist, but all four `scenario_*_generator.py` scripts write there while
  `run_dpg_causal_synthetic.py` reads from `datasets/`. Regenerating a scenario has no effect on the runner.
- `experiments/causal_synthetic_scenarios/run_dpg_synthetic.py` cannot run: no `main()` guard, and its only
  uncommented input path is missing.
- `dpg/explainer.py` omits `GradientBoostingRegressor` from its regressor tuples while `dpg/core.py`
  includes it, so local explanations of a GB regressor emit `Class …` labels the graph cannot match.
- `evaluate_faithfulness` reports `mean_path_purity`, `mean_competitor_exposure`, and
  `mean_explanation_confidence` as permanently `0.0` — `_compute_sample_confidence` never writes those keys.
